# @np/sdk

Typed API client สำหรับเรียก `apps/api` จาก `apps/web` (และ external client ในอนาคต)

- ใช้ `fetch` + zod schema จาก `@np/types`
- มี hook helper สำหรับ TanStack Query
- รองรับ auth header injection อัตโนมัติ
