# @np/config

Shared config สำหรับ workspace ทั้งหมด

- `eslint/` — preset (base, next, nest)
- `tsconfig/` — `base.json`, `next.json`, `nest.json`, `library.json`
- `prettier/` — preset (extends ของ root)
