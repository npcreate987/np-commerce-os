# @np/types

Shared TypeScript types + Zod schemas (DTO) สำหรับใช้ทั้ง `apps/web` และ `apps/api`

## ตัวอย่างไฟล์ที่จะอยู่ที่นี่ (Phase 1)
- `src/order.ts` — `Order`, `OrderItem`, `OrderStatus`
- `src/product.ts`
- `src/payment.ts`
- `src/user.ts`
- `src/zod/*.ts` — schema สำหรับ validate input ทั้ง 2 ฝั่ง
