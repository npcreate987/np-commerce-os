# @np/ui

Shared React components (Tailwind + shadcn/ui based)

- Component พื้นฐาน (Button, Input, Sheet, Drawer, Dialog, Toast)
- Mobile-first variant
- รองรับ dark mode
- Export เป็น tree-shakeable ESM
