# scripts

สคริปต์ช่วยทำงาน — ใช้ผ่าน `pnpm tsx scripts/<name>.ts`

- `seed.ts` — สร้าง mock data
- `migrate.ts` — wrapper รัน prisma migrate
- `codegen.ts` — gen types จาก OpenAPI / Zod schema
- `pwa-icons.ts` — gen icons ครบทุกขนาดสำหรับ PWA
